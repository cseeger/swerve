<%= f = form_for @changeset, "#",
  id: "user-form"
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :url %>
  <%= text_input f, :url %>
  <%= error_tag f, :url %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
